services:
  pgvector:
    image: docker.io/ankane/pgvector
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres1234
      POSTGRES_DB: evolvai
    volumes:
      - pgvector_data:/var/lib/postgresql/data
      - ./src/main/sql/init_pgvector.sql:/docker-entrypoint-initdb.d/init_pgvector.sql
      - ./src/main/sql/init_adm_tables.sql:/docker-entrypoint-initdb.d/init_adm_tables.sql

  localai:
    image: quay.io/go-skynet/local-ai:latest
    depends_on:
      - pgvector
    ports:
      - "8000:8080"
    environment:
      - MODELS_PATH=/models
    volumes:
      - ./models:/models
    command: ["/bin/local-ai", "--threads", "4", "--context-size", "1024"]
    restart: unless-stopped

volumes:
  pgvector_data:
